<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ثبت مکان جدید - Momentum</title>
  <!-- Font Awesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Shabnam Font -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/rastikerdar/shabnam-font@v5.0.0/dist/font-face.css">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Shabnam', sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #f3e7ff 0%, #fce4ec 50%, #e8eaf6 100%);
      overflow-x: hidden;
    }

    /* Background circles */
    .background-circles {
      position: fixed;
      inset: 0;
      z-index: -10;
      overflow: hidden;
    }
    .background-circles div {
      position: absolute;
      border-radius: 50%;
      filter: blur(120px);
    }
    .circle1 { width: 400px; height: 400px; background: rgba(236,72,153,0.2); top: 10%; left: -10%; }
    .circle2 { width: 350px; height: 350px; background: rgba(139,92,246,0.15); bottom: 15%; right: -15%; }
    .circle3 { inset:0; background: linear-gradient(135deg, rgba(243,231,255,0.3), rgba(252,228,236,0.3), rgba(232,234,246,0.3)); }

    /* Card */
    .form-card {
      background: rgba(255,255,255,0.9);
      backdrop-filter: blur(15px);
      border-radius: 30px;
      box-shadow: 0 15px 30px rgba(0,0,0,0.1);
      border: 1px solid rgba(255,255,255,0.4);
      width: 100%;
      max-width: 800px;
      overflow: hidden;
    }

    .form-header {
      background: linear-gradient(90deg, #ec4899, #8b5cf6, #6366f1);
      color: white;
      text-align: center;
      padding: 30px 20px;
    }
    .form-header h1 { font-size: 2rem; margin-bottom: 5px; }
    .form-header p { font-size: 1rem; }

    /* Form content */
    .form-body {
      padding: 30px 40px;
    }

    h2.section-title {
      color: #7b1fa2;
      margin-bottom: 15px;
      font-size: 1.2rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
    }

    input, select, textarea {
      padding: 12px 15px;
      border-radius: 15px;
      border: 1px solid #d1c4e9;
      font-size: 1rem;
      outline: none;
      transition: all 0.3s;
    }

    input:focus, select:focus, textarea:focus {
      border-color: #8b5cf6;
      box-shadow: 0 0 10px rgba(139,92,246,0.2);
    }

    textarea { resize: vertical; }

    label { font-weight: bold; color: #4a148c; display:block; margin-bottom:5px; }

    /* Buttons */
    .btn {
      padding: 14px 25px;
      border-radius: 25px;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      border: none;
      transition: 0.3s;
    }

    .btn-submit {
      background: linear-gradient(90deg, #ec4899, #8b5cf6, #6366f1);
      color: #fff;
      width: 100%;
    }

    .btn-submit:hover {
      opacity: 0.9;
      box-shadow: 0 5px 15px rgba(139,92,246,0.3);
    }

    .btn-reset {
      background: #e0e0e0;
      color: #333;
      width: 100%;
    }

    .btn-reset:hover { background: #d5d5d5; }

    .btn-container { display: flex; gap: 15px; flex-wrap: wrap; }

    /* Responsive */
    @media (max-width: 700px) {
      .grid { grid-template-columns: 1fr; }
      .btn-container { flex-direction: column; }
    }
  </style>
</head>
<body>
  <div class="background-circles">
    <div class="circle1"></div>
    <div class="circle2"></div>
    <div class="circle3"></div>
  </div>

  <div class="form-card">
    <div class="form-header">
      <h1>ثبت مکان جدید</h1>
      <p>صاحب مکان می‌تواند اطلاعات مکان خود را وارد کند</p>
    </div>
    <div class="form-body">
      <form action="/locations/add" method="POST" enctype="multipart/form-data">
        
        <!-- Owner Info -->
        <div>
          <h2 class="section-title">اطلاعات صاحب مکان</h2>
          <div class="grid">
            <input type="text" name="firstName" placeholder="نام" required>
            <input type="text" name="lastName" placeholder="نام خانوادگی" required>
            <input type="text" name="nationalId" placeholder="کد ملی" required>
            <input type="email" name="email" placeholder="ایمیل" required>
            <input type="tel" name="phone" placeholder="شماره تماس" required>
          </div>
        </div>

        <!-- Location Info -->
        <div>
          <h2 class="section-title">اطلاعات مکان</h2>
          <div class="grid">
            <input type="text" name="locationName" placeholder="نام مکان" required>
            <select name="locationType" required>
              <option value="">نوع مکان</option>
              <option value="هتل">هتل</option>
              <option value="رستوران">رستوران</option>
              <option value="سالن اجتماعات">سالن اجتماعات</option>
              <option value="کافه">کافه</option>
              <option value="باشگاه">باشگاه</option>
            </select>
            <input type="number" name="capacity" placeholder="ظرفیت (تعداد نفر)" required>
            <input type="text" name="address" placeholder="آدرس کامل" required>
            <input type="text" name="latitude" placeholder="عرض جغرافیایی (Latitude)" required>
            <input type="text" name="longitude" placeholder="طول جغرافیایی (Longitude)" required>
          </div>
        </div>

        <!-- Description & Image -->
        <div>
          <label>توضیحات بیشتر</label>
          <textarea name="description" rows="4" placeholder="هر نکته مهم درباره مکان..."></textarea>
        </div>

        <div>
          <label>عکس مکان</label>
          <input type="file" name="image" accept="image/*">
        </div>

        <!-- Buttons -->
        <div class="btn-container">
          <button type="submit" class="btn btn-submit"><i class="fas fa-check-circle"></i> ثبت مکان</button>
          <button type="reset" class="btn btn-reset"><i class="fas fa-undo-alt"></i> پاک کردن فرم</button>
        </div>

      </form>
    </div>
  </div>
</body>
</html>
